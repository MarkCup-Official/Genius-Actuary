# Genius Actuary Agents

- Keep orchestration in the backend adapter layer, never in the view layer.
- Prefer extending the resource registry before building one-off admin CRUD screens.
- Reuse chart factories and design tokens instead of hardcoding ECharts options or colors.
- Preserve the black-gold token system across dark, light, and system modes.
