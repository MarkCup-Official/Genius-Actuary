# Genius Actuary

Current repository state focuses on a Python backend orchestrator skeleton and a frontend integration draft.

## Layout

- `Agent/`: product proposal and task breakdown
- `backend/`: FastAPI backend with orchestrator loop and mock MCP adapters
- `frontend/`: frontend integration notes
